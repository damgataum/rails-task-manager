<h1>List of tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= task.completed ? "☑️" : "◻️" %> <%= link_to task.title, task_path(task.id)%>
  </li>
  <% end %>
</ul>
<%= link_to "➕ Add new task", new_task_path%>
